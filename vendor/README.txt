此目录用于放置 XGrids 官方 LCC Web SDK 文件（专有库，非开源）。

获取方式：
1. 打开 https://developer.xgrids.cn 并注册 / 登录
2. 进入「下载」→「LCC Web SDK」（当前最新 v0.6.3）
3. 将下载到的 SDK 文件重命名为 lcc.js 放到本目录：
   web/vendor/lcc.js

页面会自动从 vendor/lcc.js 加载。左下角水印可通过向销售申请 appKey 去除。
